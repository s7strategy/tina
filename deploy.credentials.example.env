# Copie este arquivo para deploy.credentials.env e coloque a senha do SSH (root no VPS).
# O arquivo deploy.credentials.env NÃO é commitado no git.
#
#   cp deploy.credentials.example.env deploy.credentials.env
#   # edite deploy.credentials.env e preencha SSHPASS
#
# Opcional (padrões já funcionam se não mudar):
# TINA_VPS_HOST=root@187.77.59.83
# TINA_VPS_DIR=/var/www/tina1

SSHPASS=
