/** Altura aproximada da barra inferior mobile (atalhos + FAB + padding). Usada para faixas ao vivo e padding. */
export const MOBILE_NAV_BAR_OFFSET_PX = 96
/** Base extra quando há faixa(s) de tempo ao vivo (substitui 156 no layout antigo). */
export const MOBILE_LIVE_STRIP_BASE_OFFSET_PX = 160
